
function [c,ceq] = NonLinearConstraints(X)

global idx params;

x = X(idx.x,1);
y = X(idx.y,1);
theta = X(idx.theta,1);
v = X(idx.v,1);
w = X(idx.w,1);
dt = params.dt;



c(1) = -((x- 5)^2 + (y-0)^2 - 2^2);
c(2) = v - params.
ceq = [];
end