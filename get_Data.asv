function data = get_Data()
%start position
data.start_ce

%end position

%object
data.center = [5 0];
data.radius = 2;
n = 100;
point = linspace(0, 2*pi,n);

X = data.radius * cos(point) + data.center(1);
Y = data.radius * sin(point) + data.center(2);

plot(X,Y)
grid on
data.x = X;
data.y = Y;



end

